{
  "name": "@mymediabrains/root",
  "private": true,
  "scripts": {
    "start": "lerna exec --scope @mymediabrains/app -- yarn start",
    "test": "lerna run test --scope={@mymediabrains/app,@mymediabrains/components}",
    "new-version": "lerna version --conventional-commits --yes"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "devDependencies": {
    "craco": "^0.0.3",
    "lerna": "^4.0.0"
  }
}
